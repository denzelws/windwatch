import { Story, Meta } from '@storybook/react'
import ContentSection from '.'

export default {
  title: 'ContentSection',
  component: ContentSection
} as Meta

export const Default: Story = () => <ContentSection />
