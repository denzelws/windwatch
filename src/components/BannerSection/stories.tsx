import { Story, Meta } from '@storybook/react'
import BannerSection from '.'

export default {
  title: 'BannerSection',
  component: BannerSection
} as Meta

export const Default: Story = () => <BannerSection />
